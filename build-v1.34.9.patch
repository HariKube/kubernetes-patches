diff --git a/build/lib/release.sh b/build/lib/release.sh
index 1bf19257d53..e48ed6ffbe0 100644
--- a/build/lib/release.sh
+++ b/build/lib/release.sh
@@ -337,6 +337,8 @@ function kube::release::create_docker_images_for_server() {
         rm -rf "${docker_build_path}"
         mkdir -p "${docker_build_path}"
         ln "${binary_file_path}" "${docker_build_path}/${binary_name}"
+        ln "${binary_dir}/kubectl" "${docker_build_path}/kubectl"
+        ln LICENSE "${docker_build_path}/LICENSE"
 
         local build_log="${docker_build_path}/build.log"
         if ! DOCKER_CLI_EXPERIMENTAL=enabled "${DOCKER[@]}" buildx build \
diff --git a/build/server-image/Dockerfile b/build/server-image/Dockerfile
index 78e01c36474..79478946990 100644
--- a/build/server-image/Dockerfile
+++ b/build/server-image/Dockerfile
@@ -19,4 +19,16 @@ ARG BINARY
 
 
 FROM "${BASEIMAGE}"
+LABEL name="HariKube"
+LABEL vendor="inspirNation Bt."
+LABEL version="1.34.9"
+LABEL release="0"
+LABEL summary="Kubernetes HariKube Edition"
+LABEL description="Kubernetes HariKube edition is a horizontally scalable Kubernetes edition."
+LABEL maintainer="richard.kovacs@harikube.com"
+COPY LICENSE /licenses/LICENSE
+RUN mkdir -p /tmp
+RUN chown -R 65534:0 /tmp
+RUN chmod -R 775 /tmp
+USER 65534
 COPY ${BINARY} /usr/local/bin/${BINARY}
diff --git a/build/server-image/kube-apiserver/Dockerfile b/build/server-image/kube-apiserver/Dockerfile
index d5ac37d14f1..2b272dd6261 100644
--- a/build/server-image/kube-apiserver/Dockerfile
+++ b/build/server-image/kube-apiserver/Dockerfile
@@ -26,5 +26,18 @@ COPY ${BINARY} /${BINARY}
 RUN setcap cap_net_bind_service=+ep /${BINARY}
 
 FROM --platform=linux/$TARGETARCH ${BASEIMAGE}
+LABEL name="HariKube"
+LABEL vendor="inspirNation Bt."
+LABEL version="1.34.9"
+LABEL release="0"
+LABEL summary="Kubernetes HariKube Edition"
+LABEL description="Kubernetes HariKube edition is a horizontally scalable Kubernetes edition."
+LABEL maintainer="richard.kovacs@harikube.com"
+COPY LICENSE /licenses/LICENSE
+RUN mkdir -p /var/run/kubernetes /etc/kubernetes/pki /tmp
+RUN chown -R 65534:0 /var/run/kubernetes /etc/kubernetes /tmp
+RUN chmod -R 775 /var/run/kubernetes /etc/kubernetes /tmp
+USER 65534
 ARG BINARY
 COPY --from=0 /${BINARY} /usr/local/bin/${BINARY}
+COPY --chmod=755 kubectl /usr/local/bin/kubectl
diff --git a/hack/lib/version.sh b/hack/lib/version.sh
index ffd8bc3789a..1fcd81f7290 100644
--- a/hack/lib/version.sh
+++ b/hack/lib/version.sh
@@ -91,7 +91,7 @@ kube::version::get_version_vars() {
         # git describe --dirty only considers changes to existing files, but
         # that is problematic since new untracked .go files affect the build,
         # so use our idea of "dirty" from git status instead.
-        KUBE_GIT_VERSION+="-dirty"
+        KUBE_GIT_VERSION+=""
       fi
 
 
